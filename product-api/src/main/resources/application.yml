spring:
  application:
    name: product-api

# swagger-ui custom path
springdoc:
  show-actuator: true
  swagger-ui:
    path: /swagger-ui
  api-docs:
    path: /api-docs

management:
  server:
    port: 9000
  endpoints:
    web:
      exposure:
        include: "*"
